Идея для логирования режима --headless
if hasattr(options, 'arguments'):  # возвращает True если аргумент существует у объекта
    logger.info(f'Enable {options.arguments[0]} mode')
===========================================================================================
261.5
87.5